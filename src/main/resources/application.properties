# qits-platform-docs — the reading surface over qits-artifacts' docs repository.
#
# This service holds NO state: no datasource, no Flyway, no persistence unit, no cache. Everything
# it answers it asks qits-artifacts for, which is why the whole of its configuration is one address
# and two deadlines.

# The one thing it talks to, INCLUDING the repository segment. It is the same value qits-ci injects
# into a publishing step as $QITS_DOCS_URL, deliberately: a deployment then configures one address
# and the publisher and the reader cannot disagree about where documentation lives.
#
# The in-network alias is right here for the reason the npm and maven roots give: this process
# dials it from inside qits-net, so a host-published mapping (a local stack's localhost:8081) must
# NOT be substituted — this container has no such address.
qits.platform-docs.artifacts-url=http://qits-artifacts:8080/artifacts/docs/docs

# Connect fast, read patiently. The read deadline bounds the response HEAD rather than the transfer
# — a bundle's largest file is streamed, not buffered — so it can be short enough that an
# unreachable store is a prompt 502 without making a large font a timeout.
qits.platform-docs.connect-timeout=PT2S
qits.platform-docs.request-timeout=PT30S

# Everything is served under the /platform-docs segment, because qits-gateway routes verbatim by
# prefix and rewrites nothing — there is no unprefixed spelling, on qits-net either. The segment is
# derived from this repository's own name by QitsService and is a literal in DocsPaths; no config
# key moves it.
quarkus.http.non-application-root-path=/platform-docs/q

# The readiness endpoint the deployment's health gate curls: /platform-docs/q/health/ready.
# It is the stock one deliberately — this service has no state whose health could differ from "the
# process is up", and a check that dialled qits-artifacts would take this container down for an
# outage it is designed to survive by answering 502.

# OTLP to qits-observability. The extension instruments; these two make it arrive, since the
# defaults are gRPC to localhost:4317 and the receiver speaks HTTP/protobuf.
quarkus.otel.exporter.otlp.endpoint=http://qits-observability:8080
quarkus.otel.exporter.otlp.protocol=http/protobuf
quarkus.otel.service.name=qits-platform-docs

# Dark in dev and test, the platform's convention: a suite must dial nothing.
%dev.quarkus.otel.sdk.disabled=true
%test.quarkus.otel.sdk.disabled=true

# The native binary is `qits-platform-docs`, not `qits-platform-docs-runner`, so the process is
# identifiable in `ps` and `docker top`. This name is spelled a SECOND time in docker/Dockerfile's
# COPY line, and the two move together — a mismatch is a runtime stage that copies a path that does
# not exist, which fails the image build rather than shipping something broken, but fails it after
# the multi-minute native compile.
quarkus.package.output-name=qits-platform-docs

# THE SECOND HALF, and it is not optional: `output-name` alone still appends `-runner`, so the
# native build emits qits-platform-docs-runner and the runtime stage's COPY fails on a path that
# does not exist — after the native compile has already been paid for. Both siblings carry this pair;
# copying only the first line is how this was found. The key says `jar.` and governs the native
# executable's name too.
quarkus.package.jar.add-runner-suffix=false
