# How qits-platform-deployments deploys this repo. It reads this file from the git host at
# the built sha, so the answer travels with the commit.
#
# CARRYING THE FILE AT ALL is the other thing it says. qits-workspaces reads it on a release to
# decide whether this repository deploys — a library or an SPA carries none and its release is
# the trunk push alone — and then promotes onto the branch it is configured with
# (`qits.workspaces.release.entry-branch`). `deploy_branches` used to name that branch here, in
# thirteen copies of one platform-wide answer, and a release pushed onto every entry it named.
#
# ENVIRONMENT service: it reads the docs repository of its OWN environment's qits-artifacts. It
# was platform-scoped only because qits-artifacts was, and the byte-plane split ended that: the
# store is an environment service again, so a reader per environment is one front door per store,
# not two onto one.
deployment_target: environment

# The default would be /docs/q/health/ready, derived from the name with the qits-
# prefix dropped — which is exactly right here. It is spelled anyway because the segment is
# spelled in three other places (DocsPaths.BASE, quarkus.http.non-application-root-path, and
# QitsService.DOCS in the gateway), so a reader chasing a failing health gate finds
# it here too.
health_path: /docs/q/health/ready
